name: MiniLM reranker candidate OCI proof

on:
  push:
    branches:
      - candidate/ms-marco-minilm-l6-v2-reranker
    paths:
      - ".github/workflows/poc-ms-marco-minilm-l6-v2-reranker.yml"
      - "sources/ms-marco-minilm-l6-v2-reranker.json"
      - "schemas/**"
      - "tools/harvest_reranker_minilm.py"
      - "tools/hydrate.py"
      - "poc/ms-marco-minilm-l6-v2-reranker.publish"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

env:
  PYTHONUNBUFFERED: "1"
  HF_HUB_OFFLINE: "1"
  TRANSFORMERS_OFFLINE: "1"
  OCI_REPOSITORY: ghcr.io/sempersupra/model-artifact-foundry
  LOGICAL_ID: rerank/cross-encoder/ms-marco-minilm-l6-v2
  UPSTREAM_REVISION: 233902d25c440f23af6f7d6e94d2946bac0bee0a

jobs:
  qualify-publish-pullback-hydrate:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout exact candidate branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0

      - name: Verify bounded source/base state
        shell: bash
        run: |
          set -euo pipefail
          test "$(cat poc/ms-marco-minilm-l6-v2-reranker.publish)" = "${UPSTREAM_REVISION}"
          git merge-base --is-ancestor 6622753fd5914be87fb1b6d987ceb7cae46c7ff5 HEAD
          python3 -m py_compile tools/harvest_reranker_minilm.py tools/hydrate.py

      - name: Set up ORAS
        uses: oras-project/setup-oras@1d808f7d7f6995cc68b7bf507bfe5c5446e1dc9d
        with:
          version: 1.3.3

      - name: Derive exact CPU runtime cache key
        id: runtime-key
        shell: bash
        run: |
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import sys
          print(f"python_tag={sys.implementation.cache_tag}")
          PY

      - name: Restore exact reranker runtime
        id: runtime-cache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: .reranker-venv
          key: foundry-reranker-minilm-v1-${{ runner.os }}-${{ runner.arch }}-${{ steps.runtime-key.outputs.python_tag }}-torch2.14.0-transformers5.16.1-jsonschema4.25.1

      - name: Build exact CPU runtime on cache miss
        if: steps.runtime-cache.outputs.cache-hit != 'true'
        shell: bash
        run: |
          set -euo pipefail
          python3 -m venv .reranker-venv
          .reranker-venv/bin/python -m pip install --disable-pip-version-check --no-input \
            --index-url https://download.pytorch.org/whl/cpu \
            "torch==2.14.0"
          .reranker-venv/bin/python -m pip install --disable-pip-version-check --no-input \
            "transformers==5.16.1" \
            "jsonschema==4.25.1"

      - name: Validate exact source declaration
        shell: bash
        run: |
          set -euo pipefail
          .reranker-venv/bin/python - <<'PY'
          import json
          from pathlib import Path
          import jsonschema
          schema = json.loads(Path("schemas/source-declaration.schema.json").read_text())
          doc = json.loads(Path("sources/ms-marco-minilm-l6-v2-reranker.json").read_text())
          jsonschema.validate(doc, schema)
          assert doc["logical_id"] == "rerank/cross-encoder/ms-marco-minilm-l6-v2"
          assert doc["source"]["discovery_ref"] == "233902d25c440f23af6f7d6e94d2946bac0bee0a"
          PY

      - name: Reacquire exact revision and build deterministic candidate bundle
        shell: bash
        run: |
          set -euo pipefail
          .reranker-venv/bin/python tools/harvest_reranker_minilm.py prepare \
            --dist-dir dist \
            --cache-dir /tmp/msmarco-minilm-reranker-source

      - name: Login to GHCR
        shell: bash
        env:
          GHCR_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          printf '%s' "${GHCR_TOKEN}" | oras login ghcr.io \
            --username "${GITHUB_ACTOR}" \
            --password-stdin

      - name: Publish candidate OCI
        id: publish
        shell: bash
        run: |
          set -euo pipefail
          SHORT_REV="${UPSTREAM_REVISION:0:12}"
          TAG="candidate-rerank-msmarco-minilm-l6-v2-${SHORT_REV}"
          REF="${OCI_REPOSITORY}:${TAG}"
          oras push "${REF}" \
            --artifact-type application/vnd.sempersupra.model-artifact.v1 \
            --annotation "org.opencontainers.image.source=https://github.com/${GITHUB_REPOSITORY}" \
            --annotation "org.opencontainers.image.revision=${GITHUB_SHA}" \
            --annotation "org.opencontainers.image.title=${LOGICAL_ID}" \
            dist/model.tar.gz:application/vnd.sempersupra.model.tar+gzip \
            dist/bundle-manifest.json:application/vnd.sempersupra.model.manifest.v1+json \
            dist/NOTICE.txt:text/plain
          DIGEST="$(oras resolve "${REF}")"
          [[ "${DIGEST}" =~ ^sha256:[a-f0-9]{64}$ ]]
          echo "digest=${DIGEST}" >> "${GITHUB_OUTPUT}"
          echo "ref=${REF}" >> "${GITHUB_OUTPUT}"

      - name: Pull back immutable digest and verify bundle plus model function
        shell: bash
        run: |
          set -euo pipefail
          rm -rf pullback
          mkdir pullback
          oras pull "${OCI_REPOSITORY}@${{ steps.publish.outputs.digest }}" --output pullback
          .reranker-venv/bin/python tools/harvest_reranker_minilm.py verify-pulled --bundle-dir pullback/dist

      - name: Create candidate evidence
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence
          .reranker-venv/bin/python tools/harvest_reranker_minilm.py candidate-manifest \
            --dist-dir dist \
            --oci-repository "${OCI_REPOSITORY}" \
            --digest "${{ steps.publish.outputs.digest }}" \
            --tool-revision "${GITHUB_SHA}" \
            --output evidence/candidate.json

      - name: Exercise generic hydrator twice without catalog promotion
        id: hydrate
        shell: bash
        run: |
          set -euo pipefail
          .reranker-venv/bin/python tools/harvest_reranker_minilm.py temporary-catalog \
            --candidate evidence/candidate.json \
            --output /tmp/reranker-test-catalog.json
          rm -rf /tmp/foundry-reranker-hydrate
          MODEL1="$(.reranker-venv/bin/python tools/hydrate.py \
            --catalog /tmp/reranker-test-catalog.json \
            --id "${LOGICAL_ID}" \
            --cache-root /tmp/foundry-reranker-hydrate \
            --oras "$(command -v oras)" | tail -n1)"
          .reranker-venv/bin/python tools/harvest_reranker_minilm.py smoke-model --model-dir "${MODEL1}"
          MODEL2="$(.reranker-venv/bin/python tools/hydrate.py \
            --catalog /tmp/reranker-test-catalog.json \
            --id "${LOGICAL_ID}" \
            --cache-root /tmp/foundry-reranker-hydrate \
            --oras /bin/false | tail -n1)"
          test "${MODEL1}" = "${MODEL2}"
          .reranker-venv/bin/python tools/harvest_reranker_minilm.py smoke-model --model-dir "${MODEL2}"
          echo "model_dir=${MODEL2}" >> "${GITHUB_OUTPUT}"

      - name: Record bounded candidate receipt
        shell: bash
        env:
          OCI_DIGEST: ${{ steps.publish.outputs.digest }}
          OCI_REF: ${{ steps.publish.outputs.ref }}
        run: |
          set -euo pipefail
          .reranker-venv/bin/python - <<'PY'
          import json, os
          from pathlib import Path
          receipt = {
              "schema_version": 1,
              "terminal_state": "PASS_CANDIDATE_PUBLISHED",
              "logical_id": os.environ["LOGICAL_ID"],
              "upstream_exact_revision": os.environ["UPSTREAM_REVISION"],
              "oci_repository": os.environ["OCI_REPOSITORY"],
              "oci_ref": os.environ["OCI_REF"],
              "oci_digest": os.environ["OCI_DIGEST"],
              "tool_revision": os.environ["GITHUB_SHA"],
              "pullback_verified": True,
              "generic_hydrator_first_pass": True,
              "generic_hydrator_second_offline_pass": True,
              "offline_reranker_smoke_after_hydration": True,
              "approved_catalog_promoted": False,
          }
          Path("evidence/hydration-receipt.json").write_text(json.dumps(receipt, indent=2, sort_keys=True) + "\n")
          PY

      - name: Persist candidate evidence on evidence branch
        id: evidence
        shell: bash
        run: |
          set -euo pipefail
          EVIDENCE_DIR="candidates/rerank-ms-marco-minilm-l6-v2/${UPSTREAM_REVISION}/${GITHUB_SHA}"
          BRANCH="evidence/reranker-minilm-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          mkdir -p "${EVIDENCE_DIR}"
          cp evidence/candidate.json "${EVIDENCE_DIR}/candidate.json"
          cp evidence/hydration-receipt.json "${EVIDENCE_DIR}/hydration-receipt.json"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git switch -c "${BRANCH}"
          git add "${EVIDENCE_DIR}"
          git commit -m "evidence: record MiniLM reranker candidate proof"
          git push origin "HEAD:refs/heads/${BRANCH}"
          echo "branch=${BRANCH}" >> "${GITHUB_OUTPUT}"
          echo "sha=$(git rev-parse HEAD)" >> "${GITHUB_OUTPUT}"
          echo "path=${EVIDENCE_DIR}" >> "${GITHUB_OUTPUT}"

      - name: Candidate proof summary
        shell: bash
        run: |
          {
            echo "## MiniLM reranker candidate proof"
            echo
            echo "- logical id: \`${LOGICAL_ID}\`"
            echo "- upstream revision: \`${UPSTREAM_REVISION}\`"
            echo "- OCI digest: \`${{ steps.publish.outputs.digest }}\`"
            echo "- evidence branch: \`${{ steps.evidence.outputs.branch }}\`"
            echo "- evidence commit: \`${{ steps.evidence.outputs.sha }}\`"
            echo "- approved catalog promoted: \`false\`"
            echo
            echo "Candidate qualification/publication evidence is not catalog approval."
          } >> "${GITHUB_STEP_SUMMARY}"

      - name: Upload bounded evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: foundry-reranker-minilm-candidate-${{ github.run_id }}
          path: |
            evidence/candidate.json
            evidence/hydration-receipt.json
            dist/bundle-manifest.json
            dist/NOTICE.txt
          if-no-files-found: warn
          retention-days: 7
          compression-level: 9
